import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "SmartTaskAI - Intelligent Task Manager",
  description: "Your AI-powered task manager and productivity assistant",
  keywords: ["task management", "AI", "productivity", "planning", "organization"],
  authors: [{ name: "SmartTaskAI Team" }],
  viewport: "width=device-width, initial-scale=1",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
